#include <stdio.h>
 
void pares_decrescente(int n) {
    if (n < 0) return;
    printf("%d ", n);
    pares_decrescente(n - 2);
}
 
int main() {
    int n;
    printf("Digite N (par): ");
    scanf("%d", &n);
    pares_decrescente(n);
    printf("\n");
    return 0;
}
