#include <stdio.h>
 
long long fatorial(int n) {
    if (n == 0 || n == 1) return 1;
    return n * fatorial(n - 1);
}
 
int main(void) {
    int n;
    printf("Digite N: ");
    scanf("%d", &n);
    printf("Fatorial de %d = %lld\n", n, fatorial(n));
    return 0;
}
