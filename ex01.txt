#include <stdio.h>
 
int soma(int n){
 if(n<=1)
  return n;
 return n+soma(n-1);
}

int main(){
 int n;
 printf("Digite um valor maior que 1:");
 scanf("%d",&n);
 printf("Soma=%d", soma(n);
 return 0;
}
