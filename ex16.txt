#include <stdio.h>
 
long long fatorial_duplo(int n) {
    if (n <= 1) return 1;
    return n * fatorial_duplo(n - 2);
}
 
int main(void) {
    int n;
    printf("Digite N (inteiro impar positivo): ");
    scanf("%d", &n);
    printf("%d!! = %lld\n", n, fatorial_duplo(n));
    return 0;
}
