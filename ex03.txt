#include <stdio.h>
 
int soma_cubos(int n) {
    if (n == 1)
      return 1;
    return (int)n * n * n + soma_cubos(n - 1);
}
 
int main() {
    int n;
    printf("Digite N: ");
    scanf("%d", &n);
    printf("Soma dos cubos = %d",soma_cubos(n));
    return 0;
}
