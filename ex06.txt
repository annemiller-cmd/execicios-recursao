#include <stdio.h>
 
int multip_rec(int n1, int n2) {
    if (n2 == 0) return 0;
    return n1 + multip_rec(n1, n2 - 1);
}
 
int main(void) {
    int n1, n2;
    printf("Digite n1 e n2: ");
    scanf("%d %d", &n1, &n2);
    printf("%d * %d = %d\n", n1, n2, multip_rec(n1, n2));
    return 0;
}
