#include <stdio.h>
 
long long potencia(int k, int n) {
    if (n == 0) return 1;
    return k * potencia(k, n - 1);
}
 
int main(void) {
    int k, n;
    printf("Digite k e n: ");
    scanf("%d %d", &k, &n);
    printf("%d^%d = %lld\n", k, n, potencia(k, n));
    return 0;
}
