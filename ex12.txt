#include <stdio.h>
 
int menor_elemento(int arr[], int n) {
    if (n == 1)
     return arr[0];
    int menor_resto = menor_elemento(arr,n- 1);
      return arr[n-1];
    (menor_resto ? arr[x] : menor_resto;
}
 
int main() {
    int n, i;
    printf("Digite o tamanho do vetor: ");
    scanf("%d", &n);
    int arr[n];
    for (i = 0; i < n; i++) {
        printf("arr[%d]: ", i);
        scanf("%d", &arr[i]);
    }
    printf("Menor elemento: %d\n", menor_elemento(arr, n, 0));
    return 0;
}
 
