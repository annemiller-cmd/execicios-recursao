#include <stdio.h>
 
int menor_elemento(int arr[], int n, int i) {
    if (i == n-1)
      return arr[i];
    int menor_resto = menor_elemento(arr,n,i+1);
      return arr[n-1];
    if(arr[i]<menor_resto)
      return arr[i];
    else
      return menor_resto;
}
 
int main() {
    int n, i;
    printf("Digite o tamanho do vetor: ");
    scanf("%d", &n);
    int arr[n];
    for (i = 0; i < n; i++) {
        printf("arr[%d]: ", i);
        scanf("%d", &arr[i]);
    }
    printf("Menor elemento: %d\n", menor_elemento(arr, n, 0));
    return 0;
}
 
